# Copy this file to ".env" and fill in your real OpenAI API key.
# Never commit your actual .env file to version control.

OPENAI_API_KEY=sk-your-api-key-here

# --- Optional overrides (defaults are defined in config.py) -----------------
# CHUNK_SIZE=500
# CHUNK_OVERLAP=100
# EMBEDDING_MODEL=text-embedding-3-small
# TOP_K=3
# LLM_MODEL=gpt-4o
# LLM_TEMPERATURE=0
# LLM_MAX_TOKENS=1000
# LLM_REQUEST_TIMEOUT=60

# --- Authentication ---------------------------------------------------------
ADMIN_USERNAME=shreyas
ADMIN_PASSWORD=Shreyas_2005
USER_USERNAME=user
USER_PASSWORD=user@aptus123

# --- Embedding backend ("openai" or "local") --------------------------------
# EMBEDDING_BACKEND=openai
# LOCAL_EMBEDDING_MODEL=all-MiniLM-L6-v2

